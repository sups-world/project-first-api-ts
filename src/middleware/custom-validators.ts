// import { ExpressValidator } from 'express-validator';
// import { getSingleUser } from '../services/users.services';

// export const { body } = new ExpressValidator({
//   isIdExist: async (a: string) => {
//     const ref = await getSingleUser(a);
//     if (!ref) {
//       throw new Error("This user doesn't exist");
//     }
//   },
//   isEmailExist: async (a: string) => {
//     const ref = await getSingleUser(undefined, a);
//     if (!ref) {
//       throw new Error("This email doesn't exist");
//     }
//   },
// });

// export const {} = new ExpressValidator({});
